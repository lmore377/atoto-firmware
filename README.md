# Collection of Atoto firmware files

ISPBOOOT.BIN files can be used to recover a bricked radio. Some files are split across multiple zip files to get around GitHubs max file size limit. For those files, download all the zip files in their respective folders then use software like 7zip to extract.
